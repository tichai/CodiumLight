<div class="nav-menu">
    <ul>
        {{#foreach navigation}}
            <li class="page_item page-item-2 {{#if current}} current_page_item{{/if}}" role="presentation"><a href="{{url absolute="true"}}">{{label}}</a></li>
        {{/foreach}}
    </ul>
</div>
<span class="nav-cover"></span>
